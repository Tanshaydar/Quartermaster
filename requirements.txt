fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
mcp>=1.2
PySide6>=6.11
fastembed>=0.4      # offline CPU embeddings for hybrid semantic search
numpy>=2.0
pillow>=10.0      # image loading for the vision pass (already a fastembed transitive dep)
# Optional — required only for store login & library refresh:
playwright>=1.48
